# 📋 Emby 美化全家桶 · 项目复盘与需求梳理（v2 从零开始）

> 角色：项目经理 · 编制：虾子🦐 · 日期：2026-08-11
> 状态：**待主人评审** · 评审通过后进入本地验证阶段

---

## 一、项目事故复盘（为什么 v1 废了）

### 1.1 时间线（黑历史回顾）

| 时间 | 事件 | 问题 |
|---|---|---|
| 08-10 16:30 | 主人给 token，建仓推 v1 | 正常起步 |
| 08-11 07:02 | 主人报"轮播依旧没有" | 容器内跑的是旧增强版 main.js，官方版没部署成功 |
| 07:06-07:24 | 排查：发现 md5 不一致 → 推官方版 → playwright 实测恢复 | **但没意识到 recreate 会丢** |
| 07:31 | 主人手动重建容器测试 | 修复被冲掉 → "轮播又没了" |
| 07:34-07:47 | 找到根因：**开心版镜像内置旧版 + 容器 recreate 丢修改** → 做持久化 | 开始引入复杂度 |
| 08:42 | 主人发现 UNRAID 标签丢了 | 我手动 docker run 重建时漏了 labels |
| 08:46-09:17 | 连续 5 次推仓库修复（菜单过滤/官方版检测/--reset） | **边改边推，仓库全是黑历史** |
| 09:18 | 主人忍无可忍：**仓库先关闭，本地验证清楚再说** | 仓库已删除 ✅ |

### 1.2 五个核心教训

1. **❌ 没验证就推**：每次改动只在"看起来对"就 push，没有完整的回归测试
2. **❌ 把 GitHub 当草稿纸**：7 次推送 7 次修复，仓库历史全是反复修改
3. **❌ 自作主张动生产环境**：重建容器、改 UNRAID 标签没先确认
4. **❌ 需求没对齐**：主人要的是"稳定可用的一键脚本"，我陷入了"修 bug 循环"
5. **❌ 复杂度失控**：为救火加了持久化/品牌/检测，但没有整体设计文档

### 1.3 技术根因（必须记住的 3 个坑）

| # | 坑 | 本质 |
|---|---|---|
| 1 | **开心版镜像内置旧版 emby-crx**（amilys/embyserver） | 镜像层自带注入+旧 main.js，新容器天然"已安装"但轮播不工作 |
| 2 | **容器 recreate 丢可写层修改** | 直接改 /system/dashboard-ui 的文件，重建即消失 |
| 3 | **检测逻辑只看 index.html 有没有 marker** | 无法区分"官方版已装"和"镜像自带旧版" |

---

## 二、需求梳理（从零开始，重新定义）

### 2.1 一句话定位

> **一个针对开心版 Emby（amilys 镜像）的美化全家桶脚本：本地一键安装、可靠卸载、彻底初始化，且不因容器重建而丢失。**

### 2.2 用户画像与场景

| 场景 | 用户 | 需求 |
|---|---|---|
| 主 Emby（4.9.5，家庭影音） | 主人 | 首页轮播美化，简洁不花哨 |
| emby-18（4.8.11，R18 库） | 主人 | 首页轮播 + 毛玻璃主题 + 详情页增强 |
| emby-302（4.8.11，网盘 Strm 库） | 主人 | 轮播 + 自定义品牌 Logo/Favicon |
| 未来：分享给朋友 | 其他用户 | 一条命令装好，不需要懂技术 |

### 2.3 核心需求（Must Have）

| 编号 | 需求 | 验收标准 |
|---|---|---|
| R1 | 一键安装首页美化（按 Emby 版本自动推荐） | 4.8→emby-crx，4.9→home-beautify，装完刷新即见轮播 |
| R2 | 检测准确 | 未装=无标注 / 已装官方版=[已安装] / 镜像自带旧版=[⚠️需重装] |
| R3 | 容器重建不丢 | 持久化到 /config + ext.sh 钩子，recreate 后自动恢复 |
| R4 | 可靠卸载 | --only 卸单个 / --all 卸全部（含清持久化） |
| R5 | 彻底初始化 --reset | 恢复镜像原始 index.html + 删全部美化 + 清持久化 + 删钩子 |
| R6 | 不破坏 UNRAID 标签 | 任何操作不得影响 UNRAID Docker 管理 |

### 2.4 重要需求（Should Have）

| 编号 | 需求 | 说明 |
|---|---|---|
| S1 | CSS 主题多选叠加 | 毛玻璃/极简/暗色系，与首页美化共存 |
| S2 | 功能增强组件 | 弹幕/豆瓣评分/剧照/倍速/第三方播放器等 |
| S3 | 自定义品牌（Logo/Favicon） | 用户给图片 URL，脚本下载注入 |
| S4 | 支持多容器 | --container 指定，适配多 Emby 环境 |

### 2.5 暂不做（Won't Have v2）

- ❌ 不做 Chrome 扩展版（只做服务端注入版，覆盖面更广）
- ❌ 不做在线更新检测（保持简单）
- ❌ 不做图形界面（CLI 足够）

---

## 三、开源项目调研（GitHub 实测数据）

### 3.1 竞品一览

| 项目 | Stars | 定位 | 部署方式 | 借鉴点 |
|---|---|---|---|---|
| **Nolovenodie/emby-crx** | 1252★ | 首页轮播+悬浮美化 | 插件版 + 服务器版（wget+注入 index.html） | 行业标杆，注入方式简单直接 |
| **heichaowo/Emby-Fluent** | 9★ | Fluent 风格增强 | 插件版 + 服务器版 | 功能描述清晰：轮播 10s/毛玻璃标签/字体栈/双模部署 |
| **v1rusnl/Embymalism** | 11★ | 极简 CSS 主题 | **Emby Custom CSS 粘贴**（Dashboard→General） | 主题走官方 Custom CSS 通道，不碰 index.html！ |
| **XingyiHua2024/Emby-Javascript-Details** | 43★ | 详情页增强（JAV） | JS 注入 index.html | 我们已集成 |
| **BenZuser/Emby-Web-Dark-Themes-CSS** | 51★ | 暗色系主题 | Custom CSS | 我们已集成 10 色 |
| **w282184872/emby-crx-** | 0★ | 一键脚本 | shell 脚本 | 思路与我们一致 |

### 3.2 关键架构借鉴

```
方案A（emby-crx 官方）：wget 下载文件 → 注入 </head> 前
方案B（Embymalism）：纯 CSS → 走 Emby Custom CSS 通道（不碰文件系统！）
方案C（我们 v1）：docker cp 推文件 → 注入 → 持久化
```

**💡 重要发现：Embymalism 的 Custom CSS 通道**——纯主题（不涉及 JS）可以走 Emby 官方 Custom CSS 设置，**不需要注入 index.html、不需要持久化**，Emby 重启也不丢（存在系统库）。这能大幅简化"主题"类组件的复杂度！

### 3.3 组件来源与授权

| 组件 | 来源 | 授权状态 |
|---|---|---|
| emby-crx / home-beautify | Nolovenodie/emby-crx | 开源，媒体库封面原创有版权警告⚠️（需注意） |
| emby-fluent | heichaowo/Emby-Fluent | 开源 |
| Embymalism | v1rusnl/Embymalism | 开源 |
| JAV 详情页 | XingyiHua2024 | 开源，已集成 |
| 弹幕 | chen3861229/dd-danmaku | 开源，官方服务端版 |
| 暗色主题 | BenZuser | 开源 |

> ⚠️ **版权风险**：emby-crx 明确警告"媒体库封面为原创设计，未经授权请勿模仿"。公开仓库需谨慎，可考虑仅保留功能性代码、去掉原创封面素材。

---

## 四、技术方案（v2 目标架构）

### 4.1 组件分层

```
emby-beautify/
├── install.sh          # 主安装器（交互 + CLI 参数）
├── uninstall.sh        # 卸载器（--only / --all / --reset）
├── lib/
│   ├── common.sh       # 公共函数（注入/检测/持久化/备份）
│   └── manifest.sh     # 组件注册表（声明式，新增组件只改这里）
├── styles/             # 首页美化（三选一，按版本）
├── themes/             # CSS 主题（可叠加）
├── features/           # 功能增强
└── scripts/
    └── online-install.sh  # 在线安装入口
```

### 4.2 核心机制设计（吸收教训）

| 机制 | v2 设计 | 解决什么 |
|---|---|---|
| **检测** | 注入检测 + 版本特征检测（main.js 是否官方版） | 镜像自带旧版误判 |
| **持久化** | 官方版三件套存 /config + ext.sh 钩子（exit 0 前） | recreate 丢修改 |
| **主题通道** | **优先走 Emby Custom CSS**（不注入、不持久化） | 简化主题复杂度 |
| **备份** | 每次注入前自动备份 index.html | 可回滚 |
| **--reset** | 镜像原始层恢复 + 清持久化 + 删钩子 | 彻底初始化 |
| **幂等** | marker 检测，重复安装不重复注入 | 安全重跑 |

### 4.3 验证策略（v2 必须做到）

```
本地验证环境：
  1. 干净容器（docker create 临时容器）→ 测安装
  2. 三台真机 → 测兼容
  3. recreate 容器 → 测持久化
  4. --reset → 测初始化
  5. 每项输出验证报告，主人确认后才考虑上传
```

---

## 五、验收清单（主人检查用）

- [ ] R1 一键安装轮播（4.8/4.9 各一台实测）
- [ ] R2 三种状态检测准确
- [ ] R3 recreate 后美化自动恢复
- [ ] R4 --only / --all 卸载干净
- [ ] R5 --reset 彻底初始化（含清持久化）
- [ ] R6 UNRAID 标签不受影响
- [ ] S1 主题叠加不冲突
- [ ] S3 品牌 Logo/Favicon 注入 + 持久化
- [ ] 组件来源授权清晰，无版权风险素材

---

## 六、待主人决策

1. **组件范围**：27 个组件全保留？还是精简到核心 10 个左右？
2. **版权处理**：emby-crx 的原创封面素材是否保留？（公开仓库有风险）
3. **主题通道**：是否采用"纯主题走 Custom CSS"方案（更简单可靠）？
4. **重新上传**：本地验证通过后，用新仓库名重新上传？（如 emby-beautify-tool）
5. **分享范围**：公开 / 私有？

---

*本报告基于：GitHub API 实测调研（6 个项目）+ 本地代码备份分析 + 本次事故全记录。请主人评审，我根据反馈修订后进入本地验证阶段。*
